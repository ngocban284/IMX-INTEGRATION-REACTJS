import * as TE from 'fp-ts/TaskEither';
export declare const addEthHeaders: (headers: any, ethAddress: string, ethSignature: string) => any;
export declare function signMessageFactory(signFn: (a: string) => Promise<string>, ethAddress: () => Promise<string>, shouldSign: any): (message: string) => TE.TaskEither<Error, {
    message: string;
    ethSignature: string;
    ethAddress: string;
}>;
export declare function signMessage(data: string, getEthAddress: () => Promise<string>, sign: (a: string) => Promise<string>): TE.TaskEither<Error, {
    message: string;
    ethAddress: string;
    ethSignature: string;
}>;
